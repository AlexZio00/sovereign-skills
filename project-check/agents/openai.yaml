name: project-check
description: "Scan an existing project across four dimensions — Infrastructure, Security, Quality, Harness — and report gaps ordered by severity. Read-only."
allow_implicit_invocation: true
